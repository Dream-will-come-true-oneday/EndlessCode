# Endless-Code CLI 对话助手 Spec

## 背景

当前项目处于初始阶段，已有基础框架：
- 配置系统（`config.py`）：支持从 YAML 文件加载多供应商配置
- Provider 抽象层（`providers/base.py` 和 `providers/openai.py`）：定义了统一的异步流式接口

**要解决的问题：** 缺少用户交互界面和完整的对话流程，用户无法实际使用该工具与 AI 进行对话。

## 目标

构建一个命令行交互式对话助手，让用户能够：
1. 在终端启动程序后进入友好的对话界面
2. 输入问题并获得 AI 的流式回复（逐字显示）
3. 进行多轮对话，AI 能够记住上下文
4. 通过配置文件灵活切换不同的 AI 供应商（OpenAI、DeepSeek）

**本阶段聚焦：** 纯对话功能，不包含 tool use、文件操作、代码编辑等 Agent 能力。

## 功能需求

### F1: 配置管理
系统从 YAML 配置文件加载 AI 供应商信息，支持配置多个供应商并指定当前激活的供应商。配置包含以下字段：
- `active`: 当前激活的供应商名称
- `providers`: 供应商列表，每个供应商包含：
  - `name`: 供应商标识名
  - `protocol`: 协议类型（openai 或 deepseek）
  - `model`: 模型名称
  - `base_url`: API 端点地址
  - `api_key`: API 密钥，支持环境变量引用（`$ENV_VAR`）或明文
  - `thinking`: 是否启用扩展思考模式（布尔值）

配置文件查找顺序：
1. 当前目录 `./endless-code.yaml`
2. 用户配置目录 `~/.config/endless-code/config.yaml`

### F2: 启动与初始化
用户在终端运行 `endless-code` 命令后：
- 加载并验证配置文件，若配置缺失或格式错误，显示清晰的错误信息并退出
- 加载激活的供应商配置，若环境变量未设置，显示错误并退出
- 成功后进入交互式对话界面

### F3: 交互式对话界面（TUI）
提供类似 ChatGPT 的对话记录界面：
- 用户消息显示为一条带标识的记录（如 "You:" 或用户图标）
- AI 回复流式逐字显示在用户消息下方，带 AI 标识（如 "Assistant:"）
- 历史对话向上滚动保留在屏幕上
- 底部显示输入提示符，用户可输入单行文本
- 按 Enter 提交输入
- 输入 `/exit` 或 `/quit` 或按 `Ctrl+D` 退出程序

### F4: 流式 AI 回复
调用 AI 供应商 API 时：
- 使用 SSE（Server-Sent Events）流式接收响应
- 每接收到一个 token（文字片段），立即在界面显示
- 不等待完整响应生成完毕才显示

### F5: 多轮对话与上下文记忆
在单次会话中：
- 保存所有用户输入和 AI 回复到内存中的消息列表
- 每次调用 API 时，将完整对话历史作为上下文传递
- AI 能够引用之前对话中的内容

会话生命周期：
- 启动时创建新会话
- 退出时不保存历史，下次启动从空白会话开始

### F6: 扩展思考模式支持
当供应商配置中 `thinking: true` 时：
- 在 API 请求中传递启用思考的参数（具体参数依协议而定）
- 若模型返回思考内容和最终回复，在界面中区分显示：
  - 思考内容用不同颜色或样式标识（如灰色、斜体）
  - 最终回复用正常样式显示
- 若 `thinking: false`，只显示最终回复

### F7: 错误处理
- **配置错误**：启动时检测到配置问题（文件不存在、格式错误、缺少必需字段、环境变量未设置），显示具体错误信息并退出程序
- **运行时错误**：API 调用失败、网络超时、认证失败时，在界面以红色文字显示错误消息，但不退出程序，允许用户继续输入或重试
- **网络超时**：设置合理的超时时间（如 120 秒），超时后提示用户

### F8: Provider 抽象层
- 定义统一的 `BaseProvider` 接口，包含 `stream_chat(messages)` 异步方法
- 实现 `OpenAIProvider`，支持 OpenAI 协议（适用于 OpenAI 和 DeepSeek）
- 所有 Provider 返回统一的异步迭代器，逐个 yield 文本片段
- Provider 层隔离协议差异，上层代码无需关心具体实现

## 非功能需求

### N1: 响应性能
- 流式输出延迟应在可接受范围内，用户能够感受到"打字机"效果
- 首个 token 到达时间取决于 API 响应速度，但接收到后应立即显示，无额外缓冲延迟

### N2: 用户体验
- 界面清晰易读，用户消息和 AI 回复有明显区分
- 颜色和格式增强可读性，但不过度花哨
- 错误信息具体明确，指导用户如何解决问题

### N3: 代码质量
- Provider 层职责单一，易于扩展新的 AI 供应商
- 配置加载、TUI 渲染、对话管理等模块解耦
- 使用类型提示（type hints）提高代码可维护性

### N4: 兼容性
- 支持 Python 3.11 及以上版本
- 在主流操作系统（Windows、macOS、Linux）的终端中正常工作
- 依赖库保持稳定版本（httpx、rich、prompt_toolkit、pyyaml）

### N5: 安全性
- API 密钥优先使用环境变量引用，配置示例和文档中强调此最佳实践
- 明文 API 密钥仅用于快速测试场景，文档中提醒安全风险

## 不做的事

- 工具调用 / function calling：本期不发送工具定义、不处理工具调用，纯文本对话。
- MCP 集成：不连接任何 MCP server（参考图中那行状态仅作视觉参考，不实现）。
- 权限系统：无任何需授权的操作，故不做权限确认。
- 上下文压缩：历史增长不做摘要/截断，超长由用户自行控制（留给后续章节）。
- 长期记忆 / 跨会话记忆：不做。
- 会话持久化：历史不落盘、不支持重启恢复或续聊。
- slash 命令体系：除 /exit 外，不做可扩展的命令系统（如 /help、/clear、/model 等）。
- 运行时切换 provider / model：多份配置仅在启动时选一次，会话中途不切换。
- thinking 内容展示：扩展思考增量接收即丢弃，不渲染、不折叠展示。
- 流式中断：本期不支持取消正在进行的回复。
- 自动重试 / 限流退避：出错仅提示，不自动重试。
- 其它配置来源：仅读 YAML 配置文件，不做环境变量 / 命令行 flag 覆盖。
- 用量统计：不显示 token 数与费用（但保留响应耗时计时，见 F12）。
- 多模态：不支持图片等非文本输入。

## 验收标准

### AC1: 配置加载与验证（对应 F1, F2）
- 在项目根目录创建 `endless-code.yaml`，包含两个供应商配置（OpenAI 和 DeepSeek），指定其中一个为激活供应商
- 启动程序，能够成功加载配置并进入对话界面
- 删除配置文件后启动，显示明确的"配置文件未找到"错误并退出
- 配置中 `api_key` 引用不存在的环境变量，启动时显示具体的环境变量名称和错误信息并退出

### AC2: 基本对话流程（对应 F3, F4, F5）
- 启动程序后看到欢迎信息和输入提示符
- 输入 "Hello, who are you?"，按 Enter 提交
- AI 回复以流式方式逐字显示，能看到"打字机"效果
- 继续输入 "What did I just ask you?"，AI 能够引用第一轮对话内容进行回复
- 进行 3-5 轮对话，每轮 AI 都能记住之前的上下文

### AC3: 界面展示（对应 F3）
- 用户消息和 AI 回复有明显的视觉区分（如不同颜色、标识符）
- 多轮对话后，历史消息向上滚动，新消息显示在底部
- 输入提示符始终可见且易于定位

### AC4: 退出程序（对应 F3）
- 输入 `/exit` 后程序正常退出
- 输入 `/quit` 后程序正常退出
- 按 `Ctrl+D` 后程序正常退出
- 重新启动程序，对话历史已清空，从空白会话开始

### AC5: 扩展思考模式（对应 F6）
- 配置中设置 `thinking: true`，使用支持思考模式的模型（如 DeepSeek）
- 提问需要推理的问题（如 "9.11 和 9.9 哪个大？"）
- 界面中能够看到思考内容（用不同样式标识）和最终回复
- 修改配置为 `thinking: false`，重启程序，同样的问题只显示最终回复，不显示思考过程

### AC6: 错误处理（对应 F7）
- 断开网络连接，输入问题，界面显示红色错误消息（如"网络连接失败"），但程序不退出
- 恢复网络后，能够继续输入并正常对话
- 配置中使用无效的 `api_key`，启动时或首次调用时显示认证失败错误

### AC7: 多供应商支持（对应 F1, F8）
- 配置文件中设置 `active: openai`，启动程序，能够与 OpenAI 模型对话
- 修改配置为 `active: deepseek`，重启程序，能够与 DeepSeek 模型对话
- 两种供应商的对话流程和界面体验保持一致

### AC8: Provider 抽象层验证（对应 F8）
- 代码审查：`BaseProvider` 定义了清晰的抽象接口
- 代码审查：`OpenAIProvider` 实现了该接口，处理 OpenAI 协议的 SSE 流式响应
- 代码审查：上层 CLI 代码只依赖 `BaseProvider` 接口，不直接引用具体 Provider 实现
