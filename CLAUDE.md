---
name: endless-spec
description: "Spec 驱动开发：协作澄清需求，依次生成并审批 spec.md、plan.md、task.md、checklist.md，再按文档开发和验收。开始任何功能、模块或章节开发前使用。"
---

# Spec 驱动开发

将想法逐层细化为四份文档：

```text
spec.md（做什么） -> plan.md（怎么做） -> task.md（按什么顺序做） -> checklist.md（如何确认做对）
```

<HARD-GATE>
四份文档必须依次生成并获得用户明确批准。在此之前，禁止编写实现代码；任务再小也不能跳过，但文档可按复杂度精简。
</HARD-GATE>

## 文档边界

| 文档 | 核心问题 | 内容 |
|---|---|---|
| `spec.md` | 做什么 | 背景、目标、功能与非功能需求、范围边界、验收标准 |
| `plan.md` | 怎么做 | 架构、组件、接口、数据结构、交互、文件组织、技术决策 |
| `task.md` | 按什么顺序做 | 文件清单、有序任务、依赖、操作步骤、逐项验证 |
| `checklist.md` | 如何确认做对 | 可观测行为、集成检查、工程检查、端到端场景 |

## 通用规则

- 开始前阅读相关代码、文档和近期提交，确认现状与改动边界。
- 澄清时一次只问一个关键问题，优先使用选择题。
- 若需求横跨多个独立子系统，先拆成子项目。
- 需要选型时提出 2-3 个方案；推荐方案置顶并说明取舍。
- 每份文档基于已批准的上一份文档生成，批准前不得进入下一阶段。
- `spec.md` 和 `checklist.md` 描述行为；实现细节进入 `plan.md` 和 `task.md`。
- 只设计和实现 `spec.md` 明确要求的内容（YAGNI）。
- 所有结论必须有验证证据，不能以“应该没问题”代替执行结果。

## 阶段一：需求澄清 -> spec.md

**输入：** 用户的初步想法  
**输出：** `spec.md`

### 流程

1. 了解现有项目和本次改动背景。
2. 逐项澄清目的、目标用户、成功标准、范围边界，以及性能、兼容性和安全等约束。
3. 必要时提出 2-3 个方案并给出推荐。
4. 依次确认：背景与目标、功能需求、非功能需求、不做的事、验收标准。
5. 完成自检后，请用户明确批准。

### 模板

```markdown
# [标题] Spec

## 背景
[问题、动机和现状]

## 目标
- ...

## 功能需求
- F1: ...
- F2: ...

## 非功能需求
- N1: ...

## 不做的事
- ...

## 验收标准
- AC1: ...
- AC2: ...
```

### 规则与自检

- 聚焦系统行为，保持语言和实现无关；不写函数名、类型定义或文件路径。
- 每条功能需求至少对应一条可观测的验收标准。
- 不得保留 `TBD`、`TODO` 或未完成章节。
- 消除歧义，确保范围能在一个实现周期内完成。

审批重点：需求是否完整、边界是否明确、验收标准是否可观测。未获批准则修改后重新审批。

## 阶段二：技术设计 -> plan.md

**输入：** 已批准的 `spec.md`  
**输出：** `plan.md`

### 流程

1. 为每条功能需求确定架构归属。
2. 定义组件、核心数据结构、接口和模块依赖。
3. 描述调用链、数据流和文件组织。
4. 记录关键技术决策及理由。
5. 分段确认后，请用户明确批准。

### 模板

````markdown
# [标题] Plan

## 架构概览
[组件划分及职责]

## 核心数据结构与接口
### [名称]
[字段、签名和用途]

## 模块设计
### [模块 A]
- 职责：...
- 对外接口：...
- 依赖：...

## 模块交互
[调用顺序与数据流]

## 文件组织
```text
project/
└── path/to/module.ext  # 职责
```

## 技术决策
| 决策点 | 选择 | 理由 |
|---|---|---|
| ... | ... | ... |
````

### 规则与自检

- 本文档可与语言和框架相关，接口需完整到足以独立实现模块。
- 文件组织细化到目录和文件级别。
- 每条 `F` 需求必须有架构归属；模块依赖应明确且无环。
- 检查技术决策是否与 `spec.md` 冲突。

审批重点：架构、接口、模块交互和技术决策。未获批准则修改后重新审批。

## 阶段三：任务拆解 -> task.md

**输入：** 已批准的 `spec.md`、`plan.md`  
**输出：** `task.md`

### 流程

1. 列出需要新建、修改和测试的文件。
2. 将组件拆成有序、聚焦的工作单元，目标粒度为 2-5 分钟。
3. 为每项任务写明文件、依赖、具体步骤和验证方式。
4. 检查覆盖与依赖后，请用户明确批准。

### 模板

````markdown
# [标题] Tasks

## 文件清单
| 操作 | 文件 | 职责 |
|---|---|---|
| 新建 | `path/to/file` | ... |

## T1: [任务名]
**文件：** `path/to/file`  
**依赖：** 无

**步骤：**
1. ...
2. ...

**验证：** 运行 `[命令/操作]`，预期 `[可观测结果]`

## 执行顺序
```text
T1 -> T2 -> T3
```
````

### 规则与自检

- 文件定位到文件级别并描述改动位置，避免易过期的行号。
- 任务必须自包含；执行者不依赖上下文猜测即可完成。
- 每项任务都必须有明确依赖和“运行 X，看到 Y”式验证。
- 所有计划组件均有任务覆盖，依赖链无环，名称与 `plan.md` 一致。
- 不得使用“类似 T1”等模糊引用，也不得保留占位符。

审批重点：任务粒度、依赖关系和实现覆盖。未获批准则修改后重新审批。

## 阶段四：验收设计 -> checklist.md

**输入：** 已批准的 `spec.md`、`plan.md`、`task.md`  
**输出：** `checklist.md`

### 流程

1. 将 `spec.md` 的每条验收标准转换为至少一个检查项。
2. 从 `plan.md` 补充集成和架构验证点。
3. 添加编译、测试、lint 等工程检查。
4. 至少设计一个完整的端到端场景。
5. 完成自检后，请用户明确批准。

### 模板

```markdown
# [标题] Checklist

## 功能与集成
- [ ] [功能行为]（验证：执行 X，看到 Y）
- [ ] [模块交互]（验证：运行集成测试，结果为 Y）

## 工程检查
- [ ] 项目编译通过（验证：运行 ...）
- [ ] 单元测试通过（验证：运行 ...）
- [ ] lint 通过，如项目已配置（验证：运行 ...）

## 端到端
- [ ] [用户操作] -> [可观测结果]（验证：...）
- [ ] [边界输入] -> [预期行为]（验证：...）
```

### 规则与自检

- 每项必须可执行、可观测，并附验证方式。
- 聚焦外部行为，重命名文件或移动函数不应导致检查项失效。
- 完整覆盖 `spec.md` 的验收标准，并至少包含一个端到端流程。

审批重点：验收覆盖、可观测性和端到端场景。未获批准则修改后重新审批。

## 阶段五：开发

四份文档全部获批后，明确宣告：“四份文档已全部通过审批，按 `task.md` 开始开发。”

1. 为全部任务建立进度追踪，按依赖顺序执行。
2. 每项任务完成后立即运行其验证步骤；通过后才能标记完成。
3. 验证失败时先修复并重跑；遇到文档未覆盖的阻塞则停下询问。
4. 每个任务或一组逻辑相关任务完成后提交代码。

除非被阻塞，不偏离 `task.md`；不得凭推测报告完成状态。

## 阶段六：验收

1. 逐项执行 `checklist.md` 的验证方式。
2. 记录实际结果和证据，如命令输出或观察到的行为。
3. 对失败项修复并重新验证。
4. 提交最终报告：通过数、失败项的预期与实际、修复情况，以及端到端结果。

```markdown
## 验收报告

### 通过（N/M）
- [x] 条目 — 证据：...

### 未通过
- [ ] 条目 — 预期：...；实际：...；修复：...

### 端到端
- [x] 场景 — 结果：...
```

## 禁止跳步

- “任务很简单”不构成跳过文档的理由；文档可短，但必须存在并审批。
- 不得直接从想法跳到代码或从 `spec.md` 跳到实现。
- `checklist.md` 必须在编码前完成，它验证需求，测试则验证代码。
- 不确定用户意图时先问；不确定实现状态时先运行验证。