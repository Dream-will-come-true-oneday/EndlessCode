# Agent Loop Spec

## 背景

endless-code 当前已经具备流式多轮对话和六个文件、搜索、命令工具，但 Agent 编排仍是单轮闭环：第一次模型响应可以请求一批工具，工具结果回灌后只再请求模型一次；如果第二次响应继续请求工具，这些调用不会执行。复杂任务因此需要用户反复催促，Agent 无法根据中间结果自主推进。

本阶段为 endless-code 增加完整 Agent Loop，使模型可以在同一用户回合内持续“判断 -> 调用工具 -> 查看结果 -> 调整行动”，直到完成任务或命中明确的停止条件。现有 DeepSeek 与 OpenAI 配置、TUI 对话和工具能力必须继续可用。

## 目标

- 支持模型在一个用户回合内自主完成多步工具任务。
- 为自然完成、迭代上限、异常、未知工具和用户取消提供明确且可恢复的停止行为。
- 通过统一异步事件向界面暴露文本、工具、进度和用量，不让界面依赖循环内部实现。
- 在保证调用顺序的前提下并发执行可安全并发的只读工具。
- 提供只调研不改动的 Plan Mode，并允许用户随后切换为执行模式。
- 保持 DeepSeek 与 OpenAI 两种协议路径的行为一致。

## 功能需求

- F1: ReAct 主循环
  每轮模型响应都被完整收集。若响应包含工具调用，则执行工具、回灌结果并继续下一轮；若响应不包含工具调用，则将纯文本作为最终答复并结束当前用户回合。后续轮次中的工具调用不得被丢弃。

- F2: 明确的停止条件
  Agent Loop 必须支持以下停止条件，并在停止后给界面明确结果且允许继续对话：
  1. 模型自然完成，不再请求工具。
  2. 达到内置迭代上限。
  3. 用户取消当前回合。
  4. 模型连续多轮只请求未知工具并达到内置阈值。
  5. 模型响应流发生错误。

- F3: 统一异步事件流
  Agent 仅通过事件向界面报告当前回合状态。事件必须覆盖文本增量、工具调用开始、工具调用结束及结果、Token 用量、迭代进度、停止通知、正常结束和错误。

- F4: 流式响应双路处理
  文本增量必须实时传递给界面，同时在后台累积本轮完整文本和完整工具调用。分片到达的工具参数必须正确拼接，供循环判断和执行使用。

- F5: 保序分批执行工具
  工具分为只读和有副作用两类。一次模型响应中的工具调用按原顺序扫描：连续只读调用可并发执行，有副作用调用必须串行执行；所有工具结果必须按模型原始调用顺序回灌和展示。

- F6: 对话历史一致性
  每轮 assistant 响应、工具调用和工具结果必须按顺序写入历史。无论自然完成、上限停止、未知工具停止、取消或流错误，历史都不得留下未配对的工具调用，并且后续用户消息可以继续正常请求模型。

- F7: 取消当前回合
  流式状态下，用户可通过 Esc 或 Ctrl+C 取消当前 Agent Loop，停止后续迭代并回到空闲状态，不退出程序。空闲状态下 Ctrl+C 退出程序。已经发起但未完成的工具调用必须得到可识别的取消结果。

- F8: Token 用量统计
  从每次模型响应中取得输入和输出 Token 用量，并累加为当前会话用量。界面必须展示累计输入和输出 Token 数。

- F9: 迭代进度展示
  Agent Loop 运行期间，界面必须展示当前迭代轮次，并随循环推进更新。

- F10: Plan Mode
  `/plan` 进入计划模式。该模式只向模型提供只读工具，允许模型调研并产出执行计划，但不能写文件、编辑文件或执行命令。`/do` 退出计划模式，立即基于上文计划启动执行，并恢复全部工具。模式在用户再次切换前跨回合保持。

- F11: DeepSeek 与 OpenAI 一致性
  DeepSeek 和 OpenAI（包括 OpenAI 兼容端点）都必须支持完整 Agent Loop，包括多轮工具调用、结果回灌、Token 用量、错误恢复和用户取消。

## 非功能需求

- N1: 工具超时与退出状态
  每个工具继续使用内置超时。超时必须停止对应工具及其子进程；命令非零退出、超时和其他执行失败必须作为结构化错误回灌，不得被展示为成功。

- N2: 界面响应性
  模型流、多轮循环和工具执行期间，界面必须持续响应，计时、迭代进度和运行状态能够更新。

- N3: 展示顺序
  跨轮文本、工具调用、工具结果和最终答复按真实发生顺序进入 scrollback。并发只读批次的开始、结束和结果仍按模型调用顺序展示，不得交错错位。

- N4: 结果体量控制
  大文件、长命令输出和大量搜索结果继续受工具级行数与字符数上限约束，并明确标注截断。

- N5: 取消与资源清理
  取消或超时后不得残留仍在运行的 Agent task、工具 task、队列或子进程。会话应能立即开始下一回合。

- N6: 并发安全
  并发只读工具不得共享可变执行状态；结果收集必须稳定、可重复且保持原始顺序。

- N7: 密钥安全
  配置中的 API 密钥不得出现在启动输出、错误信息、工具参数预览、工具结果摘要或对话界面中。

- N8: 兼容性与工程质量
  endless-code 必须在项目声明支持的 Python 和依赖版本范围内正常启动。格式检查、静态检查和自动化测试必须通过，测试需在 Windows 与常见 POSIX 环境使用可用的命令。

## 不做的事

- 不增加工具执行前的权限确认或审批系统。
- 不限制工具只能访问当前工作目录，也不增加文件系统沙箱。
- 不实现上下文压缩、历史裁剪或 Token 预算自动停止。
- 不持久化对话、工具结果、计划或模式状态，进程退出后仍会丢失。
- 不将迭代上限、未知工具阈值或工具超时改为用户配置项。
- 不跨越有副作用工具重排调用以追求更高并发。
- 不增加子 Agent、任务分解工具或计划审批工具。
- 不支持多模态输入输出，也不流式输出工具结果。
- 不增加新的模型协议；本阶段只覆盖现有 DeepSeek 与 OpenAI 路径。

## 验收标准

- AC1: 给出需要至少两步工具、且第二步依赖第一步结果的任务，Agent 无需用户中途催促即可连续执行并在纯文本最终答复后停止。(F1)
- AC2: 模型首轮返回无工具调用的纯文本时，Agent 立即结束且不再发起模型请求。(F2)
- AC3: 模型持续请求工具时，达到迭代上限后停止、展示原因，随后仍可继续对话。(F2)
- AC4: 模型连续只请求未知工具达到阈值时停止并展示原因；中间出现已注册工具时连续计数重置。(F2)
- AC5: 任一模型响应流出错时，当前回合停止并展示错误，程序不退出，下一条正常消息仍可处理。(F2)
- AC6: 一次包含工具调用的完整运行可观察到文本、工具开始与结束、用量、迭代、停止或结束事件；界面仅依赖这些事件完成渲染。(F3)
- AC7: 模型以多个流分片返回文本和工具参数时，文本实时可见，最终工具参数完整且可执行。(F4)
- AC8: 一次响应包含“只读、只读、有副作用、只读”调用时，前两个只读工具并发，后续调用按边界顺序执行；展示与回灌结果保持原始顺序。(F5)
- AC9: 自然完成、迭代上限、未知工具停止、流错误和执行中取消后，对话历史均无悬空工具调用；继续发送消息不会因历史格式非法而失败。(F6)
- AC10: 流式状态下 Esc 和 Ctrl+C 均取消当前回合并返回空闲状态；空闲状态 Ctrl+C 退出程序。(F7)
- AC11: 多轮 Agent Loop 期间，会话累计输入和输出 Token 数随每次模型请求增长并显示在界面中。(F8)
- AC12: 多轮 Agent Loop 期间，界面显示的迭代轮次从第一轮开始随请求递增。(F9)
- AC13: `/plan` 后只能观察到只读工具和计划文本；`/do` 后立即恢复全部工具并开始按计划执行。(F10)
- AC14: 使用 DeepSeek 和 OpenAI 配置分别运行同一多步任务，两者都能完成触发、执行、回灌、用量统计、取消和错误恢复。(F11)
- AC15: 在项目声明支持的依赖范围内启动 TUI，可正常挂载、交互和退出，不因框架内部属性冲突而崩溃。(N8)
- AC16: 命令非零退出被标记为错误；命令超时后其子进程不再继续执行或产生延迟副作用。(N1/N5)
- AC17: 工具参数或结果中包含模拟 API 密钥时，界面和错误输出均不出现该密钥原文。(N7)
